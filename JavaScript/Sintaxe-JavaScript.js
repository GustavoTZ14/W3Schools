/* Sintaxe JavaScript
 *
 * A sintaxa JavaScript é o conjuto de regras de como os programas JavaScript
 * são construidos.
*/

// criação da variavel
var x;
let y;

// usando a variavel
x = 5;
y = 6;
let z = x + y;

5 * 10 // o calculo é chamado de avaliação

x * 10 // expressões podem conter valores variáveis

"John" + "" + "Doe" // é avaliado como "John Doe"

// let palavra-chave informa ao navegador para criar vairáveis

let x, y;
x = 5 + 6;
y = x * 10;

// var palavra-chave também informa o navegador para criar variáveis

var x, y;
x = 5 + 6;
y = x * 10;

// As variáveis lastName e lastname, são duas variáveis direferentes

let lastname, lastName;
lastName = "Doe";
lastname = "Peterson";

Nome, Sobrenome, MasterCard, InterCity // unir variáveis em um unico nome.
