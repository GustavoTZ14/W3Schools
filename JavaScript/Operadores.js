// Operador de atribuição

let x = 10; // x é atribuido a 10

// Operador de adição

let x = 5;
let y = 2;
let z = x + y; // z é atribuido a x mais y

// Operador de multiplicação

let x = 5
let y = 2
let z = x * y // z é atribuido a x multplicado por y

// Operador de Atribuição de Adição

let x = 10;
x += 5;
x = y
x += x
x = x + y

x -= y
x = x - y

x *= y
x = x * y

x /= y
x = x / y

// Operadores de comparação também podem ser usados em strings

let text1 = "A";
let text2 = "B";
let result = text1 < text2;

// O operador de soma + também pode ser usado para concatenar strings

let text1 = "John";
let text2 = "Doe";
let text3 = text1 + "" + text2;

// O operador += também pode ser usado para concatenar strings

let text1 = "What a very";
text1 += "nice day";
// resultado --- what a very nice day

// Adicionar dois números retornará a soma, mas uma string e um número, 
// retornará uma string.

let x = 5 + 5 // 10 number
let y = "5" + 5; // 55 string
let z = "Hello" + 5; // Hello5 string
